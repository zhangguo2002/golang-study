1.什么是CSP?
CSP(通信顺序进程)并发编程模型，它的核心思想是：通过通信共享内存，而不是通过共享内存通信。Go语言的Goroutine和Channel机制，就是CSP的经典实现，具有以下特点：
1、避免共享内存：协程（Goroutine）不直接修改变量，而是通过channel通信
2、天然同步：channel的发送/接受自带同步机制，无序手动加锁
3、易于组合：channel可以嵌套使用，构建复杂并发模式（如管道、超时控制）